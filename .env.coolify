# Coolify Production Environment Configuration
# Copy these values to Coolify's environment variables settings

# Database Configuration
POSTGRES_USER=testuser
POSTGRES_PASSWORD=testpass123
POSTGRES_DB=testdb
DATABASE_URL=postgresql://testuser:testpass123@postgres:5432/testdb

# Backend Configuration
# IMPORTANT: Generate a secure key for production!
# Use: openssl rand -hex 32
JWT_SECRET_KEY=super-secret-key-change-in-production-min-32-chars
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Frontend Configuration
# CRITICAL: This must be the external URL where your backend is accessible
NEXT_PUBLIC_API_URL=https://api.madeindhaka.com

# Environment
ENVIRONMENT=production

# Coolify-Specific Notes:
# ----------------------
# 1. Set these variables in Coolify's UI under "Environment Variables"
# 2. NEXT_PUBLIC_API_URL must be the publicly accessible backend URL
# 3. For PR previews, this URL will automatically be set by Coolify
# 4. Database service name 'postgres' works for internal communication
# 5. Don't expose ports in docker-compose.yml (use 'expose' instead)
